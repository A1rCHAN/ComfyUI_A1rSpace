# ComfyUI A1rSpace Dependencies
# Note: Core dependencies (torch, numpy, pillow, safetensors, transformers, kornia, etc.) 
# are provided by ComfyUI and are not listed here.

# API Integration (Translation & Text Generation)
requests>=2.28.0          # For Baidu/DeepSeek API calls
openai>=1.0.0             # For OpenAI-compatible API calls

# JoyTag / WD14 Tagging Support
huggingface-hub>=0.15.0   # For downloading tagging models
onnxruntime>=1.15.0       # For running ONNX tagging models (WD14)